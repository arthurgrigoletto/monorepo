name: Build & Deploy (Production)

on:
  push:
    branches:
      - master
    paths:
      - 'packages/server/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkou@v2

      - name: Setup Node.js
        users: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: Deploy
        run: echo "Deployed"
